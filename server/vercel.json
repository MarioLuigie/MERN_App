{
  "version": 2,
  "builds": [
    { "src": "server/package.json", "use": "@vercel/node" }
  ],
  "routes": [
    { "src": "/api/posts/(.*)", "dest": "server/routes/posts.js" },
    { "src": "/(.*)", "dest": "client/$1" }
  ]
}

